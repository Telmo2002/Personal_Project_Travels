<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil - Travels</title>
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel="stylesheet" href="/stylesheets/sidebar.css"> <!-- Link para o CSS externo -->
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
    }
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      width: 15%;
      height: 100%;
      background-color: #00bcd4; /* Azul ciano */
      padding-top: 20px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .sidebar h3 {
      text-align: center;
      font-size: 24px;
      color: white;
    }
    .sidebar a {
      display: block;
      padding: 10px;
      text-align: center;
      color: white;
      text-decoration: none;
      font-size: 18px;
    }
    .sidebar a:hover {
      background-color: #0097a7;
    }
    .sidebar .logout-form {
      text-align: center;
      margin-bottom: 20px;
    }
    .sidebar .logout-form button {
      background-color: #f44336;
      border: none;
      color: white;
      padding: 10px;
      font-size: 18px;
      cursor: pointer;
      border-radius: 4px;
    }
    .sidebar .logout-form button:hover {
      background-color: #d32f2f;
    }
    .page-content {
      margin-left: 15%;
      padding: 20px;
    }
    .w3-container.w3-teal {
      margin-bottom: 20px;
    }
    .profile-header {
      margin: 0;
      padding: 20px;
      background-color: #4CAF50;
      color: white;
      text-align: center;
      font-size: 28px;
      border-radius: 8px 8px 0 0;
    }
    .profile-picture {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      margin: 0 auto;
      display: block;
      border: 4px solid #4CAF50; /* Contorno verde */
    }
    .profile-info {
      margin-top: 30px;
      text-align: center;
    }
    .profile-info p {
      font-size: 18px;
      color: #333;
      margin: 10px 0;
    }
    .profile-info label {
      font-weight: bold;
      display: block;
      margin-bottom: 5px;
      color: #555;
    }
    .edit-profile-button {
      background-color: #4CAF50;
      color: white;
      padding: 12px 20px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      border: none;
      display: block;
      margin: 30px auto;
      text-align: center;
      width: 200px;
      text-decoration: none;
    }
    .edit-profile-button:hover {
      background-color: #388E3C; /* Verde mais escuro */
    }
  </style>
</head>
<body>

<!-- Sidebar -->
<div class="sidebar">
  <div>
    <h3>Menu</h3>
    <a href="/" class="w3-bar-item w3-button">Página inicial</a>
    <a href="#" class="w3-bar-item w3-button">Sobre nós</a>
  </div>
  
  <!-- Logout Button -->
  <form action="/logout" method="POST" class="logout-form">
    <button type="submit">Terminar Sessão</button>
  </form>
</div>

<!-- Page Content -->
<div class="page-content">

  <div class="w3-container w3-teal">
    <h1 class="profile-header">Perfil</h1>
  </div>

  <div class="w3-container">
    <img src="<%= user.profilePicture ? '/images/users/' + user.profilePicture : '/images/avatar.png' %>" alt="Profile Picture" class="profile-picture">
    
    <div class="profile-info">
      <p><label for="username">Username:</label> <%= user.username %></p>
      <p><label for="name">Nome Completo:</label> <%= user.name %></p>
      <p><label for="birthdate">Data de Nascimento:</label> <%= new Date(user.birthdate).toLocaleDateString() %></p>
      <p><label for="description">Descrição:</label> <%= user.description || 'Nenhuma descrição disponível' %></p>
    </div>

    <a href="/profile/edit" class="edit-profile-button">Editar Perfil</a>
  </div>

</div>

</body>
</html>
