<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Perfil - Travels</title>
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel="stylesheet" href="/stylesheets/sidebar.css"> <!-- Link para o CSS externo -->
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
    }
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      width: 15%;
      height: 100%;
      background-color: #00bcd4; /* Azul ciano */
      padding-top: 20px;
    }
    .sidebar h3 {
      text-align: center;
      font-size: 24px;
      color: white;
    }
    .sidebar a {
      display: block;
      padding: 10px;
      text-align: center;
      color: white;
      text-decoration: none;
      font-size: 18px;
    }
    .sidebar a:hover {
      background-color: #0097a7;
    }
    .sidebar .logout-form {
      text-align: center;
      margin-top: auto;
      margin-bottom: 20px;
    }
    .sidebar .logout-form button {
      background-color: #f44336;
      border: none;
      color: white;
      padding: 10px;
      text-align: center;
      font-size: 18px;
      cursor: pointer;
      border-radius: 4px;
    }
    .sidebar .logout-form button:hover {
      background-color: #d32f2f;
    }
    .page-content {
      margin-left: 15%;
      padding: 20px;
    }
    .w3-container.w3-teal {
      margin-bottom: 20px;
    }
    .profile-header {
      margin: 0;
      padding: 20px;
      background-color: #4CAF50;
      color: white;
      text-align: center;
      font-size: 28px;
      border-radius: 8px 8px 0 0;
    }
    .profile-picture {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      margin: 0 auto;
      display: block;
      border: 4px solid #4CAF50; /* Contorno verde */
    }
    .form-container {
      text-align: center;
      margin-top: 20px;
    }
    .form-container form {
      display: inline-block;
      text-align: left;
      max-width: 500px;
      width: 100%;
    }
    .form-container label {
      font-weight: bold;
    }
    .form-container input[type="text"],
    .form-container input[type="date"],
    .form-container textarea,
    .form-container input[type="file"] {
      width: 100%;
      padding: 8px;
      margin: 8px 0;
      box-sizing: border-box;
      border-radius: 4px;
      border: 1px solid #ccc;
    }
    .form-container button {
      background-color: #4CAF50;
      color: white;
      padding: 12px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      border: none;
      width: 100%;
    }
    .form-container button:hover {
      background-color: #388E3C; /* Verde mais escuro */
    }
  </style>
</head>
<body>

<!-- Sidebar -->
<div class="sidebar">
  <h3>Menu</h3>
  <a href="/" class="w3-bar-item w3-button">Página inicial</a>
  <a href="/profile" class="w3-bar-item w3-button">Perfil</a>
  <a href="#" class="w3-bar-item w3-button">Sobre nós</a>
  
  <!-- Logout Button -->
  <form action="/logout" method="POST" class="logout-form">
    <button type="submit">Terminar Sessão</button>
  </form>
</div>

<!-- Page Content -->
<div class="page-content">

  <div class="w3-container w3-teal">
    <h1 class="profile-header">Editar Perfil</h1>
  </div>

  <div class="w3-container">
    <img src="<%= user.profilePicture ? '/images/users/' + user.profilePicture : '/images/avatar.png' %>" alt="Profile Picture" class="profile-picture">
    
    <div class="form-container">
      <form action="/profile/edit" method="POST" enctype="multipart/form-data">
        <label for="name">Nome Completo:</label>
        <input type="text" id="name" name="name" value="<%= user.name %>" required>

        <label for="birthdate">Data de Nascimento:</label>
        <input type="date" id="birthdate" name="birthdate" value="<%= new Date(user.birthdate).toISOString().substr(0, 10) %>" required>

        <label for="description">Descrição:</label>
        <textarea id="description" name="description"><%= user.description %></textarea>

        <label for="profilePicture">Imagem de Perfil:</label>
        <input type="file" id="profilePicture" name="profilePicture" accept="image/*">

        <button type="submit">Guardar Alterações</button>
      </form>
    </div>
  </div>

</div>

</body>
</html>
